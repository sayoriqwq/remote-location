# Require explicit controller pairing

The iPhone app will trust a Mac Simulation Controller only after the developer enters a one-time six-digit code shown by that controller. The resulting controller identity is retained in the iPhone Keychain for automatic reconnection, providing local authentication without introducing user accounts or a cloud service.
